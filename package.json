{
  "private": true,
  "name": "preact-issue",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "dev": "preact watch",
    "start": "preact watch"
  },
  "engines": { "node": "14.x" },
  "devDependencies": {
    "preact-cli": "^3.0.0"
  },
  "dependencies": {
    "preact": "^10.3.2",
    "preact-render-to-string": "^5.1.4"
  }
}
